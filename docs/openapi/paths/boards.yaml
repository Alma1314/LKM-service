status:
  get:
    tags: [boards]
    summary: 板块模块状态
    operationId: boardsStatus
    responses:
      "200":
        description: OK
        content:
          application/json:
            schema:
              type: object
              properties:
                module: { type: string }
                status: { type: string }
                responsibility: { type: string }
                next_steps:
                  type: array
                  items: { type: string }
